# Normalize line endings to LF in the repo; Netlify builds on Linux.
* text=auto eol=lf

# Treat these as binary — never touch line endings.
*.png binary
*.ico binary
*.jpg binary
*.jpeg binary
*.webp binary
*.woff binary
*.woff2 binary
